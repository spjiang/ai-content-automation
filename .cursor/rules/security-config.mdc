---
description: 安全与配置管理规则
alwaysApply: true
---

# Security And Config Standards

- 禁止提交密钥、令牌、数据库连接串、私有证书等敏感信息。
- 所有第三方服务凭证统一通过环境变量或密钥管理系统注入。
- 日志中不得输出敏感字段；必要时对 token、手机号、邮箱做脱敏。
- 配置文件区分环境（dev/test/prod），禁止将生产配置直接用于开发环境。
- 新接入外部 API 时，必须定义超时、重试与熔断/降级策略。
- 对外错误信息保持最小披露，不返回内部堆栈与实现细节。
